import type { SxProps, Theme } from "@mui/material";

export const stepNumber: SxProps<Theme> = {
  minWidth: 24,
  color: "text.secondary",
  pt: "9px",
};

export const addButton: SxProps<Theme> = { mt: 1 };
